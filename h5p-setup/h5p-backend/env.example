# H5P Storage Configuration
# Choose one of: memory, filesystem, database, cloud
STORAGE_TYPE=memory

# File System Storage Configuration
CONTENT_DIR=./h5p/content

# Database Storage Configuration
# Supported formats:
# - SQLite: sqlite://./h5p/content.db
# - PostgreSQL: postgresql://user:password@localhost:5432/h5p_db
# - MySQL: mysql://user:password@localhost:3306/h5p_db
DATABASE_URL=sqlite://./h5p/content.db

# Cloud Storage Configuration
# Choose one of: gcp, aws, azure
CLOUD_PROVIDER=gcp
BUCKET_NAME=your-h5p-content-bucket
PROJECT_ID=your-gcp-project-id
GOOGLE_APPLICATION_CREDENTIALS=./path/to/service-account-key.json

# AWS S3 Configuration (if using AWS)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1

# Azure Blob Storage Configuration (if using Azure)
AZURE_STORAGE_CONNECTION_STRING=your-azure-connection-string
